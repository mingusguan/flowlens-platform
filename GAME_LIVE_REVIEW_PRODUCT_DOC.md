# 游戏主播复盘系统功能文档

版本：v0.2  
日期：2026-06-05

## 1. 功能模块总览

| 模块 | 优先级 | 说明 |
| --- | --- | --- |
| 主播管理 | P0 | 创建主播档案和监听配置 |
| 直播场次管理 | P0 | 云端监听到开播后自动生成 |
| 弹幕与人气采集 | P0 | 核心数据采集 |
| 互动时间轴 | P0 | 按时间展示弹幕、人气、情绪变化 |
| 游戏话题分析 | P0 | 分析观众讨论的游戏内容 |
| 活跃观众分析 | P0 | 识别高频互动用户和老粉 |
| 弹幕情绪与节奏分析 | P0 | 识别兴奋、吐槽、争议、负面 |
| 直播复盘报告 | P0 | 自动生成单场直播总结 |
| 每场直播的数据统计 | P0 | 统计直播间数据和基础指标 |
| 冷场区间识别 | P1 | 找出低互动和人气下滑区间 |
| 多场直播对比 | P1 | 对比主播多场直播表现 |
| 主播横向对比 | P1 | 对比多个主播公开数据表现 |

## 2. 主播管理

### 功能点

- 创建主播档案。
- 配置主播 liveId。
- 配置主播昵称。
- 配置是否启用云端监听。
- 配置监听频率。
- 查看主播列表。
- 查看主播当前直播状态。
- 查看主播历史直播场次。

### 实现方式

- 后端新增主播档案表。
- 用户只维护主播，不手动创建直播场次。
- 云端定时任务扫描已启用监听的主播。
- 监听任务通过 liveId 检测主播是否开播。
- 检测到开播且当前没有进行中的场次时，自动创建直播场次。
- 检测到下播后，自动结束直播场次并触发统计和报告任务。
- 前端提供主播列表页、主播详情页、监听状态页。

### 核心字段

- `id`
- `live_id`
- `anchor_name`
- `monitor_enabled`
- `monitor_status`
- `last_live_status`
- `last_check_time`
- `current_session_id`

## 3. 直播场次管理

### 功能点

- 自动生成直播场次。
- 自动绑定主播。
- 自动绑定直播间 liveId。
- 记录主播名称。
- 记录直播标题。
- 记录开播时间。
- 记录下播时间。
- 记录采集状态。
- 查看场次列表。
- 查看场次详情。
- 支持按主播、日期、场次状态筛选。

### 实现方式

- 后端新增直播场次表。
- 云端监听到主播开播时自动创建场次。
- 直播采集任务启动时绑定当前自动生成的场次。
- 通过 liveId 关联弹幕、人气和统计数据。
- 直播结束后更新场次结束时间和复盘状态。
- 前端提供场次列表页和场次详情页。

### 核心字段

- `id`
- `anchor_id`
- `live_id`
- `room_id`
- `anchor_name`
- `live_title`
- `start_time`
- `end_time`
- `duration_seconds`
- `collect_status`
- `review_status`

## 4. 弹幕与人气采集

### 功能点

- 采集弹幕消息。
- 采集用户昵称。
- 采集用户 ID。
- 采集弹幕发送时间。
- 采集当前在线人数。
- 采集累计观看人数。
- 采集直播间统计消息。
- 采集直播间状态。
- 采集中断自动记录异常。
- 直播结束自动停止采集。

### 实现方式

- 继续使用 DouyinLiveWebFetcher 监听公开直播间消息。
- `WebcastChatMessage` 入库为弹幕数据。
- `WebcastRoomUserSeqMessage` 入库为在线人数和累计观看数据。
- `WebcastRoomStatsMessage` 入库为直播间展示统计。
- `WebcastControlMessage` 用于判断直播结束。
- 云端监听任务检测到主播开播后，按 liveId 启动采集任务。
- 后端提供采集任务状态查询接口。

### 核心表

- 直播弹幕表。
- 直播人气快照表。
- 直播采集任务表。

### 核心字段

弹幕：

- `session_id`
- `live_id`
- `user_id`
- `nickname`
- `content`
- `msg_id`
- `event_time`

人气快照：

- `session_id`
- `live_id`
- `current_viewers`
- `total_viewers`
- `snapshot_time`

## 5. 互动时间轴

### 功能点

- 按分钟聚合弹幕数。
- 按分钟聚合活跃用户数。
- 按分钟展示在线人数。
- 展示弹幕量曲线。
- 展示在线人数曲线。
- 展示情绪变化曲线。
- 展示关键词标签。
- 标记高互动时间段。
- 标记低互动时间段。
- 点击时间段查看弹幕明细。

### 实现方式

- 后端按 `session_id + minute_bucket` 聚合数据。
- 弹幕量来自弹幕表 count。
- 活跃用户数来自去重 user_id count。
- 在线人数取该分钟最近一次快照。
- 关键词来自弹幕分词结果。
- 情绪来自弹幕情绪分析结果。
- 前端使用折线图、柱状图和时间轴列表展示。

### 聚合维度

- `minute_time`
- `comment_count`
- `active_user_count`
- `current_viewers`
- `viewer_delta`
- `top_keywords`
- `emotion_summary`
- `segment_tags`

### 标签规则

- 弹幕数高于本场平均值 2 倍：高互动。
- 活跃用户数高于本场平均值 2 倍：高互动。
- 弹幕数低于本场平均值 30% 且持续 5 分钟：低互动。
- 在线人数连续下降超过阈值：人气下滑。

## 6. 游戏话题分析

### 功能点

- 识别游戏关键词。
- 识别英雄、角色、地图、装备、玩法等词。
- 统计高频话题。
- 统计话题出现时间段。
- 识别争议话题。
- 识别玩梗话题。
- 支持不同游戏配置不同词库。
- 输出本场游戏话题 TOP 榜。

### 实现方式

- 当前阶段使用内置游戏互动词典和高频词抽取规则。
- 弹幕入库后按需计算话题命中、用户数和集中时段。
- 不在主播档案增加游戏、分类、标签字段。
- 后续需要细化到具体游戏时，再新增独立词库配置，不改主播基础表。
- 未命中词典的高频词可作为候选词，在后续词库后台中维护。

### 话题分类

- 英雄/角色。
- 地图/模式。
- 装备/道具。
- 上分/排位。
- 队友/对手。
- 操作/失误。
- 梗/口头禅。
- 争议/节奏。

### 核心表

- 游戏词库表。
- 弹幕分词结果表。
- 场次话题统计表。

## 7. 活跃观众分析

### 功能点

- 统计本场发言用户数。
- 统计用户发言次数。
- 统计用户首次发言时间。
- 统计用户最后发言时间。
- 统计用户参与时长。
- 识别本场活跃观众 TOP。
- 识别连续多场出现的老观众。
- 识别新增活跃观众。
- 识别近期流失活跃观众。
- 标记用户互动类型。

### 实现方式

- 按 `session_id + user_id` 聚合弹幕。
- 发言次数排序生成本场活跃榜。
- 跨场次按 `anchor_id/live_id + user_id` 统计出现次数。
- 最近 N 场都出现的用户标记为老观众。
- 当前场首次出现且发言超过阈值的用户标记为新增活跃。
- 历史活跃但最近 N 场未出现的用户标记为流失风险。
- 基于弹幕内容分类用户互动类型。

### 用户类型规则

- 发言多且正向词多：捧场型。
- 问号和疑问句多：提问型。
- 梗词多：玩梗型。
- 负面词多：吐槽型。
- 争议词、攻击词多：带节奏型。

### 输出指标

- 本场活跃用户数。
- 人均弹幕数。
- 活跃观众 TOP。
- 老观众数量。
- 新增活跃观众数量。
- 流失风险观众数量。

## 8. 弹幕情绪与节奏分析

### 功能点

- 识别正向弹幕。
- 识别兴奋弹幕。
- 识别搞笑弹幕。
- 识别吐槽弹幕。
- 识别争议弹幕。
- 识别负面弹幕。
- 按时间统计情绪变化。
- 标记争议集中区间。
- 标记负面集中区间。
- 输出典型弹幕。

### 实现方式

- 建立情绪词典。
- 弹幕入库后异步进行情绪分类。
- 简单场景先用规则和词典打标。
- 后续接入大模型或文本分类模型提升准确率。
- 按分钟聚合各类情绪数量。
- 情绪占比超过阈值时生成时间段标签。

### 情绪分类

- 兴奋：`666`、`牛`、`天秀`、`卧槽`、`厉害`。
- 搞笑：`哈哈`、`笑死`、`绷不住`。
- 吐槽：`太菜了`、`这也行`、`寄`、`逆天`。
- 争议：互喷、质疑、吵架、攻击。
- 负面：`无聊`、`看不懂`、`没意思`、`退了`。

### 输出指标

- 正向弹幕数。
- 负面弹幕数。
- 争议弹幕数。
- 情绪峰值时间段。
- 负面集中时间段。
- 典型弹幕列表。

## 9. 每场直播的数据统计

### 功能点

- 统计直播时长。
- 统计峰值在线人数。
- 统计平均在线人数。
- 统计累计观看人数。
- 统计弹幕总数。
- 统计活跃观众数。
- 统计人均弹幕数。
- 统计高互动时间段数量。
- 统计低互动时间段数量。
- 统计直播间统计消息。

### 实现方式

- 当前阶段按需从结构化事件、场次表和统计表聚合。
- 峰值在线取 `max(current_viewers)`。
- 平均在线取 `avg(current_viewers)`。
- 累计观看取最后一次 `total_viewers`。
- 活跃观众取去重 `user_id`。
- 人均弹幕数为弹幕总数 / 活跃观众数。
- 基础计数写入场次统计表；时间线、话题、观众、情绪和冷场按需计算。

### 核心指标

- `duration_seconds`
- `peak_viewers`
- `avg_viewers`
- `total_viewers`
- `comment_count`
- `active_user_count`
- `comments_per_user`
- `high_interaction_count`
- `low_interaction_count`
## 10. 直播复盘报告

### 功能点

- 自动生成单场复盘报告。
- 展示本场核心数据。
- 展示互动时间轴摘要。
- 展示高频游戏话题。
- 展示活跃观众 TOP。
- 展示情绪和争议片段。
- 展示冷场区间。
- 输出下场直播建议。
- 支持导出或复制报告。

### 实现方式

- 当前阶段打开页面时按需生成报告。
- 后端读取场次统计、时间轴聚合、话题统计、活跃观众、情绪分析、冷场识别结果。
- 使用模板生成结构化报告。
- 需要自然语言总结时，调用大模型生成摘要。
- 大模型输入只传统计结果和典型弹幕，不传原始全量弹幕。
- 后续需要历史报告留档时，再新增报告表保存生成结果。

### 报告结构

- 基础数据。
- 互动表现。
- 人气走势。
- 游戏话题。
- 活跃观众。
- 情绪节奏。
- 冷场区间。
- 下场建议。

### 报告边界

- 不写礼物贡献。
- 不写游戏操作判断。
- 不写主播话术判断。
- 不写确定性高光结论。
- 只写基于弹幕和人气的分析。

## 11. 冷场区间识别

### 功能点

- 识别弹幕明显减少的时间段。
- 识别活跃用户明显减少的时间段。
- 识别在线人数连续下降的时间段。
- 计算冷场持续时间。
- 输出冷场前后的关键词变化。
- 输出冷场前后的典型弹幕。
- 给出下场互动建议。

### 实现方式

- 基于互动时间轴的分钟级聚合数据计算。
- 设置本场平均弹幕数作为基准。
- 连续 N 分钟低于阈值则标记冷场。
- 同时参考在线人数下降和活跃用户下降。
- 冷场片段写入场次片段表。

### 默认规则

- 窗口长度：5 分钟。
- 弹幕数低于本场分钟均值 30%。
- 活跃用户数低于本场分钟均值 30%。
- 在线人数连续下降。
- 满足两项以上标记为冷场。

## 12. 多场直播对比

### 功能点

- 对比多场直播核心指标。
- 对比峰值在线变化。
- 对比平均在线变化。
- 对比弹幕总数变化。
- 对比活跃观众变化。
- 对比老观众回访变化。
- 对比高频游戏话题变化。
- 对比情绪变化。
- 对比冷场时长变化。
- 输出周报/月报。

### 实现方式

- 按主播、场次和时间范围聚合多场直播统计。
- 支持选择时间范围。
- 从结构化事件和场次统计读取核心指标。
- 按需计算高频话题变化、老观众回访、负面和争议趋势。
- 前端用折线图、柱状图、对比表展示。

### 核心指标

- 场次总数。
- 总直播时长。
- 平均峰值在线。
- 平均弹幕数。
- 平均活跃观众数。
- 老观众回访率。
- 冷场时长占比。
- 争议弹幕占比。

## 13. 主播横向对比

### 功能点

- 选择多个主播或默认对比全部主播。
- 对比在线人数。
- 对比弹幕活跃度。
- 对比单位在线弹幕率。
- 对比活跃观众数。
- 对比高频话题。
- 对比情绪和争议占比。
- 对比直播时段。
- 输出主播横向对比报告。

### 实现方式

- 基于已维护的主播档案和自动生成的直播场次对比。
- 采集多个公开直播间的弹幕和人气。
- 按主播和时间范围聚合对比，不依赖游戏、分类、标签字段。
- 单位在线弹幕率 = 弹幕数 / 平均在线人数。
- 话题对比和情绪对比按需从弹幕明细计算。
- 前端提供主播横向对比页。

### 核心指标

- 平均在线人数。
- 峰值在线人数。
- 弹幕总数。
- 单位在线弹幕率。
- 活跃观众数。
- 高频话题 TOP。
- 争议弹幕占比。
- 冷场时长占比。

## 14. 后端任务流程

### 采集流程

1. 用户创建主播档案并启用监听。
2. 云端监听任务定时检测主播开播状态。
3. 检测到开播后自动创建直播场次。
4. 采集任务绑定当前场次并开始监听。
5. 监听弹幕、人气和直播间状态。
6. 原始消息解析成结构化数据。
7. 写入业务表。
8. 检测到下播后停止采集。
9. 更新直播场次结束时间。
10. 触发统计和复盘报告任务。

### 分析流程

1. 生成分钟级互动时间轴。
2. 执行弹幕分词。
3. 执行情绪分类。
4. 聚合游戏话题。
5. 聚合活跃观众。
6. 识别冷场区间。
7. 生成单场数据统计。
8. 生成复盘报告。

## 15. 当前数据表与后续扩展

### 当前表

- `live_anchor`：主播档案和云端监听配置。
- `live_collector_task`：云端采集任务。
- `live_session`：直播场次。
- `live_event_yyyyMM`：弹幕、人气、关注、进场等结构化事件。
- `live_session_stat`：单场基础统计。

### 后续可选表

- `live_topic_word`：独立词库配置。
- `live_review_report`：复盘报告留档。
- `live_timeline_bucket`：高并发查询下的分钟级缓存。
- `live_segment`：高互动、低互动、冷场片段缓存。

## 16. 接口清单

### 主播

- `POST /api/live/anchors`
- `GET /api/live/anchors`
- `GET /api/live/anchors/{id}`
- `PUT /api/live/anchors/{id}`
- `POST /api/live/anchors/{id}/enable-monitor`
- `POST /api/live/anchors/{id}/disable-monitor`
- `GET /api/live/anchors/{id}/monitor-status`
- `GET /api/live/anchors/{id}/sessions`

### 场次

- `GET /api/live/sessions`
- `GET /api/live/sessions/{id}`

### 数据

- `GET /api/live/sessions/{id}/stats`
- `GET /api/live/sessions/{id}/timeline`
- `GET /api/live/sessions/{id}/topics`
- `GET /api/live/sessions/{id}/active-users`
- `GET /api/live/sessions/{id}/emotions`
- `GET /api/live/sessions/{id}/segments`

### 报告

- `POST /api/live/sessions/{id}/generate-report`
- `GET /api/live/sessions/{id}/report`

### 对比

- `GET /api/live/compare/sessions`
- `GET /api/live/compare/anchors`
